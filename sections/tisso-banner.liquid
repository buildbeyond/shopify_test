{% comment %}
  Banner section — Gift Guide hero content.
  Editable: title, description, button label, button link, background image.
{% endcomment %}

{{ 'section-tisso-banner.css' | asset_url | stylesheet_tag }}

<section
  id="TissoBanner-{{ section.id }}"
  class="tisso-banner"
>
  {%- if section.settings.background_image != blank -%}
    <div class="tisso-banner__media" aria-hidden="true">
      {{
        section.settings.background_image
        | image_url: width: 3840
        | image_tag:
          loading: 'eager',
          widths: '750, 1100, 1500, 2000, 3000, 3840',
          sizes: '100vw',
          alt: ''
      }}
    </div>
  {%- endif -%}

  <div class="tisso-banner__content">
    {%- if section.settings.title != blank -%}
      <h2 class="tisso-banner__title">{{ section.settings.title | escape }}</h2>
    {%- endif -%}

    {%- if section.settings.description != blank -%}
      <div class="tisso-banner__description">{{ section.settings.description }}</div>
    {%- endif -%}

    {%- if section.settings.button_label != blank -%}
      <a
        class="tisso-banner__button"
        href="{{ section.settings.button_link | default: '#' }}"
        {% if section.settings.button_link == blank %}
          aria-disabled="true"
        {% endif %}
      >
        <span class="tisso-banner__button-label">{{ section.settings.button_label | escape }}</span>
        <span class="tisso-banner__button-arrow" aria-hidden="true">→</span>
      </a>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Banner",
  "tag": "div",
  "class": "section-tisso-banner",
  "settings": [
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background image"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "The Gift Guide"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Description",
      "default": "<p>Discover Joy: Your Ultimate Holiday Gift Destination. Explore our curated selection and find the perfect gifts to delight your loved ones this holiday season.</p>"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "SHOP NOW"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    }
  ],
  "presets": [
    {
      "name": "Banner"
    }
  ]
}
{% endschema %}
